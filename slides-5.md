---
theme: seriph
background: https://cover.sli.dev
title: Git Merge & Python Multiple Inheritance
info: |
  ## Git Merge и Множественное наследование в Python
class: text-center
drawings:
  persist: false
transition: slide-left
mdc: true
seoMeta:
  ogImage: auto
---

# Добро пожаловать

---

# Git Merge и Множественное наследование

Lesson 5

---

## Git Merge - что это такое?

**Git merge** - это объединение изменений из одной ветки в другую

<v-clicks>

**Простая аналогия:**

Представьте, что вы пишете книгу с другом:
- Вы работаете над главой 1 (ваша ветка)
- Друг работает над главой 2 (его ветка)
- Когда вы оба закончили, вы **объединяете** (merge) ваши главы в одну книгу (main ветка)

</v-clicks>

---

## Git Merge - как это работает?

<v-clicks>

**Шаги для merge:**

1. Убедитесь, что все изменения сохранены (commit)
2. Переключитесь на ветку, КУДА хотите добавить изменения
3. Выполните команду merge

```bash
# Пример: хотим добавить изменения из feature в main
git switch main        # Переходим на main
git merge feature        # Объединяем feature с main
```

**Результат:** все изменения из ветки `feature` теперь в ветке `main`

</v-clicks>

---

## Git Merge - визуально

```
До merge:

main    ->:     A --- B --- C
                       \
feature ->:                D --- E


После merge (git merge feature):

main:     A --- B --- C --- F (merge commit)
                       \   /
feature:                D-E
```

<v-clicks>

- Точки A, B, C, D, E - это коммиты (сохраненные изменения)
- F - специальный коммит, который объединяет изменения
- Все изменения из feature теперь в main

</v-clicks>

---

## Конфликты при merge - что это?

<v-clicks>

**Конфликт** - это когда Git не может автоматически объединить изменения

**Аналогия:**
Вы и друг исправляете одно предложение в книге по-разному:
- Вы написали: "Сегодня солнечный день"
- Друг написал: "Сегодня дождливый день"
- Git не знает, какой вариант правильный - нужно выбрать вручную!

</v-clicks>

---

## Когда происходят конфликты?

<v-clicks>

**Конфликт возникает, когда:**

1. **Два человека изменили одну и ту же строку** в одном файле
2. **Один удалил файл, другой изменил** этот же файл
3. **Оба изменили одно и то же место** в коде

**Пример:**

```python
# В ветке main:
def hello():
    print("Привет!")

# В ветке feature:
def hello():
    print("Hello!")
```

При merge Git не знает, какой вариант оставить - возникает конфликт!

</v-clicks>

---

## Почему конфликты - это нормально?

<v-clicks>

- Конфликты - это **защита** от потери изменений
- Git говорит: "Подожди, тут два разных варианта, выбери правильный!"
- Это происходит при совместной работе над одним кодом
- **Главное:** сохраняйте работу часто (commit), общайтесь с командой

**Как выглядит конфликт в файле:**

```python
<<<<<<< HEAD
print("Привет!")
=======
print("Hello!")
>>>>>>> feature
```

</v-clicks>

---
transition: slide-up
---

# Множественное наследование в Python

Multiple Inheritance

---

## Что такое множественное наследование?

<v-clicks>

**Обычное наследование:** класс наследует от одного родителя

```python
class Animal:
    def breathe(self):
        print("Дышу")

class Dog(Animal):  # Dog наследует от Animal
    def bark(self):
        print("Гав!")
```

**Множественное наследование:** класс наследует от нескольких родителей

```python
class Dog(Animal, Pet):  # Dog наследует от Animal И Pet
    pass
```

</v-clicks>

---

## Множественное наследование - простой пример

```python
class Swimmer:
    def swim(self):
        print("Плыву 🏊")

class Flyer:
    def fly(self):
        print("Лечу ✈️")

class Duck(Swimmer, Flyer):  # Утка умеет И плавать И летать
    def quack(self):
        print("Кря-кря! 🦆")

# Использование
duck = Duck()
duck.swim()   # Плыву 🏊 (от Swimmer)
duck.fly()    # Лечу ✈️ (от Flyer)
duck.quack()  # Кря-кря! 🦆 (свой метод)
```

<v-clicks>

**Утка наследует методы от обоих родителей!**

</v-clicks>

---

## Проблема Diamond (Ромбовидная проблема)

<v-clicks>

**Что если два родителя имеют метод с одинаковым именем?**

```python
class Animal:
    def make_sound(self):
        print("издает звук")

class Cat(Animal):
    def make_sound(self):
        print("Мяу")

class Dog(Animal):
    def make_sound(self):
        print("Гав")

class Kotopes(Dog, Cat):  # Kotopes наследует от Dog И Cat
    pass

kotopes = Kotopes()
kotopes.make_sound()
```

**Ответ:** из Dog

</v-clicks>

---

## Diamond Problem - диаграмма

```
        Animal (make_sound)
       / \
      /   \
     Dog   Cat  (оба имеют make_sound)
      \   /
       \ /
        Kotopes
```

<v-clicks>

**Вопрос:** если Kotopes вызывает `make_sound()`, откуда брать метод?

Варианты пути: Kotopes → Dog → Animal или Kotopes → Cat → Animal

**Python решает это с помощью MRO** (Method Resolution Order)

</v-clicks>

---

## MRO - Method Resolution Order

<v-clicks>

**MRO** - это порядок, в котором Python ищет методы в классах

**Правило:** Python идет **слева направо** в списке родителей, затем вверх по иерархии

```python
class Kotopes(Dog, Cat):  # Сначала Dog потом Cat
    pass

print(Kotopes.__mro__)
# Или
print(Kotopes.mro())
```

**Результат:**
```
(<class 'Kotopes'>, <class 'Dog'>, <class 'Cat'>, <class 'Animal'>, <class 'object'>)
```

**Порядок поиска:** Kotopes → Dog → Cat → Animal → object

</v-clicks>

---

## MRO - для чего нужен?

<v-clicks>

**MRO гарантирует:**

1. **Предсказуемость:** всегда понятно, какой метод вызовется
2. **Без дубликатов:** каждый класс проверяется только один раз
3. **Правильный порядок:** родители всегда идут после детей

**Пример:**

```python
class Kotopes(Dog, Cat):
    pass

kotopes = Kotopes()
kotopes.make_sound()  # Ищет в порядке: Kotopes → Dog → Cat → Animal
           # Находит в Dog, вызывает make_sound
```

</v-clicks>

---

## super() в множественном наследовании

<v-clicks>

**super()** - вызывает метод следующего класса по MRO

```python
class Animal:
    def make_sound(self):
        print("Животное издает звук")

class Dog(Animal):
    def make_sound(self):
        print("Гав")
        # Вызовет следующий по MRO
        # то есть Cat
        super().make_sound()

class Cat(Animal):
    def make_sound(self):
        print("Мяу")
        # Вызовет следующий по MRO
        # то есть Animal
        super().make_sound()

class Kotopes(Dog, Cat):
    def make_sound(self):
        print("Котопес говорит:")
        # Вызовет следующий по MRO
        # то есть Dog
        super().make_sound()

kotopes = Kotopes()
kotopes.make_sound()
```

</v-clicks>

---

## super() - как работает с MRO

```python
class Animal:
    def make_sound(self):
        print("Животное издает звук")

class Dog(Animal):
    def make_sound(self):
        print("Гав")
        super().make_sound()

class Cat(Animal):
    def make_sound(self):
        print("Мяу")
        super().make_sound()

class Kotopes(Dog, Cat):
    def make_sound(self):
        print("Котопес говорит:")
        super().make_sound()

kotopes = Kotopes()
kotopes.make_sound()
```

<v-clicks>

**MRO для Kotopes:** Kotopes → Dog → Cat → Animal → object

**Вывод:**
```
Котопес говорит:
Гав
Мяу
Животное издает звук
```

</v-clicks>

---

## super() - объяснение работы

<v-clicks>

1. `kotopes.make_sound()` → вызывается `Kotopes.make_sound()` → печатает "Котопес говорит:"
2. `super().make_sound()` в Kotopes → следующий по MRO это Dog → вызывается `Dog.make_sound()` → печатает "Гав"
3. `super().make_sound()` в Dog → следующий по MRO это Cat → вызывается `Cat.make_sound()` → печатает "Мяу"
4. `super().make_sound()` в Cat → следующий по MRO это Animal → вызывается `Animal.make_sound()` → печатает "Животное издает звук"
5. В Animal нет `super().make_sound()` → конец

**super() использует MRO, чтобы найти следующий класс!**

</v-clicks>

---

## super() - практический пример

```python
class Device:
    def __init__(self, brand):
        self.brand = brand
        print(f"Device: {brand}")

class Phone:
    def __init__(self, brand, os):
        self.os = os
        print(f"Phone: {os}")
        super().__init__(brand)  # Передаем дальше по MRO

class Camera:
    def __init__(self, brand, megapixels):
        self.megapixels = megapixels
        print(f"Camera: {megapixels}MP")
        super().__init__(brand)  # Передаем дальше по MRO

class Smartphone(Phone, Camera, Device):
    def __init__(self, brand, os, megapixels):
        super().__init__(brand, os, megapixels)

phone = Smartphone("Apple", "iOS", 12)
# Вывод:
# Phone: iOS
# Camera: 12MP
# Device: Apple
```

---

## Множественное наследование - итоги

<v-clicks>

**Ключевые моменты:**

- **Множественное наследование** - класс наследует от нескольких родителей
- **MRO** - порядок поиска методов (слева направо, потом вверх)
- **Diamond Problem** - когда несколько родителей имеют одинаковый метод
- **super()** - вызывает следующий метод по MRO, не обязательно родительский
- **Проверить MRO:** `ClassName.mro()` или `ClassName.__mro__`

</v-clicks>

---

## Практические советы

<v-clicks>

**Git Merge:**
- Делайте commit часто
- Общайтесь с командой, чтобы избежать конфликтов
- Не бойтесь конфликтов - это нормальная часть работы

**Множественное наследование:**
- Используйте осторожно - может усложнить код
- Всегда проверяйте MRO при множественном наследовании
- Используйте `super()` для правильного вызова методов родителей
- Помните: порядок родителей в `class Child(Parent1, Parent2)` важен!

</v-clicks>

---
transition: slide-up
---

# Декораторы в Python

Decorators

---

## Что такое декоратор?

<v-clicks>

**Декоратор** - это функция, которая изменяет поведение другой функции

**Простая аналогия:**

Представьте себе **фильтр для фотографий в Instagram**:
- У вас есть обычная фотография (функция)
- Вы применяете фильтр "Черно-белое" (декоратор)
- Получаете ту же фотографию, но с новым эффектом!

Фильтр **не меняет саму фотографию**, он просто **добавляет эффект** сверху.

</v-clicks>

---

## Аналогия с подарком

<v-clicks>

**Декоратор - это как коробка для подарка:**

- **Подарок** - это ваша функция (самое главное)
- **Коробка** - это декоратор (красивая упаковка)
- **Результат** - тот же подарок, но в красивой упаковке!

Коробка **не меняет подарок внутри**, но делает его **более красивым** и **удобным** для передачи.

Декоратор **не меняет функцию**, но может **добавить что-то до** или **после** её работы!

</v-clicks>

---

## Самый простой пример

```python
def hello():
    print("Привет!")

hello()  # Привет!
```

<v-clicks>

**Теперь добавим декоратор:**

```python
def make_fancy(func):  # Декоратор
    def wrapper():
        print("✨" * 10)  # До функции
        func()            # Сама функция
        print("✨" * 10)  # После функции
    return wrapper

@make_fancy  # Применяем декоратор
def hello():
    print("Привет!")

hello()
```

**Вывод:**
```
✨✨✨✨✨✨✨✨✨✨
Привет!
✨✨✨✨✨✨✨✨✨✨
```

</v-clicks>

---

## Как работает декоратор?

<v-clicks>

```python
@make_fancy
def hello():
    print("Привет!")
```

**Это то же самое, что:**

```python
def hello():
    print("Привет!")

hello = make_fancy(hello)  # "Заворачиваем" функцию в декоратор
```

**Шаги:**
1. Декоратор получает функцию `hello`
2. Создает новую функцию `wrapper`, которая добавляет ✨ до и после
3. Возвращает `wrapper` вместо `hello`
4. Теперь когда мы вызываем `hello()`, на самом деле вызывается `wrapper()`

</v-clicks>

---

## Пример декоратора с измерением времени

```python
import time
from functools import wraps

def measure_time(func):
    @wraps(func)  # Сохраняет имя функции
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        end = time.time()
        print(f"⏱️  {func.__name__} выполнялась {end - start:.2f} сек")
        return result
    return wrapper

@measure_time
def slow_function():
    print("Начинаю работу...")
    time.sleep(2)
    print("Закончил работу!")

slow_function()
```

<v-clicks>

**Вывод:**
```
Начинаю работу...
Закончил работу!
⏱️  slow_function выполнялась 2.00 сек
```

</v-clicks>

---

## Декорирование функций с аргументами

```python
from functools import wraps

def log_calls(func):
    @wraps(func)  # Сохраняет имя и информацию о функции
    def wrapper(*args, **kwargs):  # Принимает любые аргументы
        print(f"🔔 Вызов: {func.__name__}")
        result = func(*args, **kwargs)  # Передаем аргументы дальше
        print(f"✅ Результат: {result}")
        return result
    return wrapper

@log_calls
def add(a, b):
    return a + b

@log_calls
def greet(name, age):
    return f"Привет, {name}! Тебе {age} лет"

add(5, 3)
print()
greet("Алиса", 12)
```

<v-clicks>

**Вывод:**
```
🔔 Вызов: add
✅ Результат: 8

🔔 Вызов: greet
✅ Результат: Привет, Алиса! Тебе 12 лет
```

</v-clicks>

---

## Где используются декораторы?

<v-clicks>

**Простые случаи:**
- **Логирование** - запись, когда функция вызвана и с какими параметрами
- **Измерение времени** - сколько времени работала функция
- **Подсчет вызовов** - сколько раз функцию вызывали
- **Валидация данных** - проверка аргументов перед выполнением

**Продвинутые случаи:**
- **Кэширование** - сохранение результатов, чтобы не вычислять повторно
- **Проверка прав доступа** - только авторизованные пользователи могут вызвать функцию
- **Retry (повтор)** - автоматический повтор при ошибке (например, при запросе к API)
- **Rate limiting** - ограничение частоты вызовов (не больше 10 раз в минуту)
- **Автоматическая транзакция БД** - начать/завершить транзакцию базы данных

</v-clicks>

---

## Декораторы - итоги

<v-clicks>

**Ключевые моменты:**

- **Декоратор** - функция, которая изменяет поведение другой функции
- Синтаксис: `@decorator_name` над функцией
- Декоратор **не меняет** саму функцию, а **оборачивает** её
- Используйте `@wraps(func)` чтобы сохранить имя декорируемой функции
- `*args, **kwargs` позволяет передавать любые аргументы в функцию

**Простое правило:** декоратор добавляет код **до** и/или **после** функции

</v-clicks>

